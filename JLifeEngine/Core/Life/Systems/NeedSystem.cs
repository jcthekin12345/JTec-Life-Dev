namespace JLifeEngine.Core.Life.Systems;

public class NeedSystem : ILifeSystem
{
    public void Update(TimeSpan deltaTime, World world)
    {
        // Update all being needs over time
        // Trigger actions when needs become urgent
    }
}